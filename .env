# =============================
# LOCAL DEVELOPMENT ENV (.env)
# DO NOT COMMIT REAL SECRETS TO GIT / NETLIFY.
# Anything starting with VITE_ or EXPO_PUBLIC_ is PUBLIC and exposed to browsers.
# =============================

# --- Server (private) ---
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.example.mongodb.net/store?retryWrites=true&w=majority
JWT_SECRET=change_this_dev_secret
PORT=5000

# --- API base for local frontend & mobile (public for dev) ---
VITE_API_BASE=https://omraneva.onrender.com 
# Backward compatibility (some code may still read VITE_API_URL)
VITE_API_URL=https://omraneva.onrender.com 

# --- Cloudinary (only keep non-secret values public) ---
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_API_KEY=your_public_api_key
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset
# DO NOT expose secret to client: remove VITE_CLOUDINARY_API_SECRET; keep server-only below
CLOUDINARY_API_SECRET=your_private_secret_value
CLOUDINARY_API_KEY=your_private_admin_key

# If you use the signed upload endpoint server-side, reference CLOUDINARY_* vars not prefixed with VITE_

# --- Public flags ---
NODE_ENV=development
DELIVERY_ADMIN_BYPASS=true
DEV_DELIVERY_NO_AUTH=true

# --- Delivery Hub (optional) ---
# DELIVERY_HUB_BASE_URL=https://delivery-hub.example.com/api
# DELIVERY_HUB_API_KEY=xxxxx

# --- Tablet redirect policy (desktop|mobile|ignore) ---
MOBILE_WEB_TABLET_POLICY=desktop

# --- Web Push VAPID (public key may be exposed; private MUST remain server-only) ---
VAPID_PUBLIC_KEY=REPLACE_PUBLIC_KEY
VAPID_PRIVATE_KEY=REPLACE_PRIVATE_KEY
VAPID_SUBJECT=mailto:admin@example.com

# --- Mobile external redirect (optional) ---
# MOBILE_WEB_EXTERNAL_URL=https://m.yourdomain.com

# --- API base for Expo (can also set EXPO_PUBLIC_API_URL) ---
EXPO_PUBLIC_API_URL=https://omraneva.onrender.com

# =============================
# Create a separate .env.production (NOT committed) or set values in hosting dashboards.
# =============================

